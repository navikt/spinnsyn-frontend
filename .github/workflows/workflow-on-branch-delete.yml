name: Demo delete
on: delete

jobs:
  branch-delete:
    uses: oivindkulsrud/flex-github-actions-workflows/.github/workflows/demo-delete.yml@main
    permissions:
      id-token: write
      contents: "read"
    with:
      base-path: syk/sykepenger
